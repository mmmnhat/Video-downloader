__pycache__/
.venv/
node_modules/
dist/
web/dist/
web/node_modules/
.DS_Store
*.pyc
downloads/
downloads-test/
.google_token.json
google_oauth_client.json
app_state.json
voices_cache.json

# Vendor binaries (too large for git — downloaded by auto_build.py)
vendor/windows/bin/*.exe

build/
packaging/windows/build/
packaging/windows/dist/

# Ignore compiled exe artifacts in dist (not spec files)
dist/*.exe
dist/

# TTS runtime & browser profiles (all of tts_profiles is local/runtime data)
tts_profiles/
tts_batches/
tts_state.json

# Playwright runtime
.playwright/
.playwright-cli/

# Tests output
tests/

# Scratch test videos
scratch/*.mp4
scratch/*.webm
